# Widźwięk worker — zależności PoC
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
pydantic>=2.6,<3.0
python-multipart>=0.0.9        # obsługa upload (multipart/form-data)

# Dev / testy
pytest>=8.0,<9.0
httpx>=0.27,<1.0               # TestClient FastAPI

# --- TBD: integracja AI (odkomentować przy realnych providerach) ---
# faster-whisper>=1.0          # ASR (Whisper) — wariant lokalny
# openai>=1.0                  # ASR — wariant API
# pyannote.audio>=3.1          # diaryzacja (wymaga tokenu HF)
# ffmpeg-python>=0.2           # ingest / ekstrakcja audio z wideo
