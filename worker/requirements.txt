# Widźwięk worker — zależności (tryb demo MOCK działa bez kluczy i bez SDK zewnętrznych API)
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
pydantic>=2.6,<3.0
python-multipart>=0.0.9        # obsługa upload (multipart/form-data)

# Dev / testy (nie wymagają żadnych kluczy ani sieci)
pytest>=8.0,<9.0
httpx>=0.27,<1.0               # TestClient FastAPI

# === MVP no-API-first (bez kluczy, darmowe/open-source) ===
faster-whisper>=1.1,<2.0       # lokalny ASR (CTranslate2/Whisper), bez płatnego API
yt-dlp>=2025.1.1               # pobieranie istniejących napisów/auto-captionów, bez pobierania wideo

# === Placeholdery integracji zewnętrznych (odkomentuj przy włączaniu danego trybu) ===
# PIPELINE_MODE=api — transkrypcja przez OpenAI (import leniwy; brak pakietu => czytelny błąd):
# openai>=1.0
# Wideo w trybie api: ekstrakcja audio przez ffmpeg (zainstaluj w systemie, dodaj do PATH).
# Opcjonalne lokalne modele jakości:
# inaSpeechSegmenter>=0.7       # speech/music/noise segmentation; cięższe zależności
# pyannote.audio>=3.1           # diarizacja; część modeli wymaga akceptacji/licencji/tokenu HF
# speechbrain>=1.0              # speaker/audio toolkit; do oceny jako provider lokalny
