# Worker (Python / FastAPI) — skopiuj do worker/.env
WIDZWIEK_HOST=127.0.0.1
WIDZWIEK_PORT=8000
WIDZWIEK_CORS_ORIGINS=http://localhost:3000

# Silniki AI: "mock" na PoC. TBD: whisper / pyannote / yamnet
WIDZWIEK_ASR_PROVIDER=mock
WIDZWIEK_DIARIZATION_PROVIDER=mock
WIDZWIEK_SOUND_PROVIDER=mock

# Zrzut wyników jobów (runtime, nie commitujemy)
WIDZWIEK_STORAGE_DIR=./storage

# Klucze do modeli (puste na PoC)
# OPENAI_API_KEY=
# HUGGINGFACE_TOKEN=
