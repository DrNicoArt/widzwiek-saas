# Web (Next.js) — skopiuj do web/.env.local
# Adres workera (Python/FastAPI). Lokalnie:
NEXT_PUBLIC_WORKER_URL=http://localhost:8000
# Na Vercel: ustaw publiczny adres workera (tunel lub serwer), patrz docs/ROADMAP.md
